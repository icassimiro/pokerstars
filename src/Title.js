import pokerstars from "./images/pokerstars.png";

const Title = () => {
  return (
    <div className="title">
      <img className="ps" src={pokerstars} alt="ps" />
    </div>
  );
};

export default Title;
